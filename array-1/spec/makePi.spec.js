/*
Return an int array length 3 containing the first 3 digits of pi, {3, 1, 4}.

- PIの最初の3桁をもつ配列を返せ

makePi() → [3, 1, 4]

*/

const makePi = () => {
  // Write code here

};

describe('makePi()', () => {

    it('makePi() -> [3, 1, 4])', () => {
      expect(makePi()).toEqual([3, 1, 4]);
    });

});
